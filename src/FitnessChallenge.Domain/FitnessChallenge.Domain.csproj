<Project Sdk="Microsoft.NET.Sdk">

  <!-- This project depends on nothing but the base class library, on purpose:
       the scoring engine must be testable without a database or an HTTP host.
       Anything needing persistence or the web framework belongs in
       FitnessChallenge.Api. See docs/contract.md. -->

</Project>
